import { BackgroundSizeDirective } from './background-size.directive';

describe('BackgroundSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundSizeDirective();
    expect(directive).toBeTruthy();
  });
});
